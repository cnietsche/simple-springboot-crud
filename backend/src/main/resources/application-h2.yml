spring:
  datasource:
    url: jdbc:h2:file:${H2_DATA_PATH:./data}/users;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: false
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
